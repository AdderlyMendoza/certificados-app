# URL base de la API. En dev se usa el proxy de Vite (/api), así que puedes dejar /api/v1.
VITE_API_BASE_URL=/api/v1
# Destino del proxy de Vite hacia el backend
VITE_API_TARGET=http://localhost:8000
